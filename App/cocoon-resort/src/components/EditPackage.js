import React, { Component } from 'react';
import { Modal, Form } from 'semantic-ui-react';

class EditPackage extends Component {
    constructor(props) {
        super(props);

        const { id, name, details, price, from, to } = this.props.info;
    }
}