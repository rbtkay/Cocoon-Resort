class Package {

    async getLocations() {
        const locations = [{ text: 'kevin', value: 'kevin' },
        { text: 'robert', value: 'robert' }];
        return locations; //TODO: fetch the locations
    }
}

export default Package;